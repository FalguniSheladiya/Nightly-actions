[project]
name = "rl-takehome"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["gymnasium>=0.29", "stable-baselines3>=2.3", "torch"]

[dependency-groups]
dev = ["pytest>=8"]

[tool.pytest.ini_options]
testpaths = ["tests"]
